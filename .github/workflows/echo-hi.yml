name: Echo Hi

on:
  workflow_dispatch:
    inputs:
      data:
        description: 'Additional data to process'
        required: false
        default: '{}'

jobs:
  echo-hi:
    runs-on: ubuntu-latest

    steps:
    - name: Processing Data
      run: echo "${{ github.event.inputs.data }}"

